import { LocationsearchPipe } from './locationsearch.pipe';

describe('LocationsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new LocationsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
